{
  "scripts": {
    "test:firestore": "firebase emulators:exec 'jest ./test/firestore'",
    "test": "jest"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.2",
    "@types/jest": "^28.1.0",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.4"
  }
}
